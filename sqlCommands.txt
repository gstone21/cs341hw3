CREATE DATABASE CHEESECAKE;
USE CHEESECAKE;
CREATE TABLE ORDERS (
  ORDERID int,
  MONTH char(3),
  DAY int,
  QUANTITY int,
  TOPPING varchar(9),
  NOTES varchar(512)
);
DESCRIBE ORDERS;
SELECT ORDERID FROM ORDERS;
SELECT * FROM ORDERS WHERE MONTH='Feb' && TOPPING='plain';
INSERT INTO ORDERS  (ORDERID, MONTH, DAY, QUANTITY, TOPPING, NOTES)
VALUES (6, 'Sep', 15, 2, 'cherry', 'very nice');